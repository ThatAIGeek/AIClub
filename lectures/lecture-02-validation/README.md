# Лекція 2. Валідація моделей та методологія експерименту

Повний комплект матеріалів до лекції (80 хв, пара).

| Файл | Що це |
|---|---|
| [`lecture-02-slides.pptx`](lecture-02-slides.pptx) | **презентація, 39 слайдів** — для PowerPoint або імпорту в Google Slides |
| [`lecture-notes.md`](lecture-notes.md) | **конспект викладача** — що казати в кожному блоці, формули, числа, розбори кейсів, відповіді на типові запитання |
| [`slides.html`](slides.html) | та сама колода одним HTML-файлом для браузера (клавіатурна навігація, нотатки, темна тема) |
| [`demo/leakage_demo.ipynb`](demo/leakage_demo.ipynb) | **демо-ноутбук** до блоку 3, показувати наживо |
| [`tools/`](tools/) | скрипт, яким зібрано `.pptx` (щоб перезібрати після правок) |

## Презентація

`.pptx` у форматі 16:9 (13,33 × 7,5 дюйма), шрифти Arial і Courier New — обидва є і в Office,
і в Google Slides, тому імпорт не перебиває розкладку.
**Нотатки доповідача вбудовані в кожен слайд** і переносяться в Google Slides разом із колодою.

Імпорт у Google Slides: Файл → Імпортувати слайди → завантажити `.pptx` → вибрати всі слайди.
Нативні діаграми (5 графіків) під час імпорту стають зображеннями — це нормально,
редагувати їх після імпорту не потрібно.

## Структура (80 хв)

| Блок | Слайди | хв |
|---|---|---|
| 0. Хук: два числа | 1–4 | 6 |
| 1. Навіщо розбивати: узагальнення і шум оцінки | 5–8 | 8 |
| 2. Схеми крос-валідації | 9–15 | 14 |
| 3. **Витік даних** (ядро) | 16–25 | 20 |
| 4. Незбалансовані класи | 26–31 | 14 |
| 5. Калібрування ймовірностей | 32–35 | 10 |
| 6. Офлайнова vs цільова метрика | 36 | 5 |
| 7. Чек-ліст + анонс ЛР2 | 37–38 | 3 |
| Додаток. Джерела | 39 | — |

Якщо пара йде 2×40 з перервою — перерва після блоку 3 (слайд 25).
Інтерактив «Знайди витік» — слайд 23; розбори всіх шести кейсів лежать у нотатках доповідача
до цього слайда і в конспекті.

## Демо-ноутбук

`demo/leakage_demo.ipynb` самодостатній: дані генеруються всередині, потрібні лише
`numpy`, `scikit-learn`, `matplotlib`. Виводи збережені, тож колоду можна показувати
й без запуску. Що він друкує:

| Експеримент | Результат |
|---|---|
| випадкове розбиття кадрів проти `GroupKFold` за місіями | ROC-AUC **0.929 → 0.594 ± 0.048** |
| куди дивилась модель | 84 % внеску — підпис місії, 16 % — справжній сигнал |
| відбір ознак до розбиття на **чистому шумі** з випадковими мітками | ROC-AUC **0.914** проти **0.483** усередині `Pipeline` |
| `Scaler + PCA` до розбиття проти тих самих кроків у `Pipeline` | **0.631** проти **0.506** |
| label-shuffle | чесний пайплайн **0.497**, пайплайн із витоком **0.965** |
| бутстреп-ДІ по місіях | 0.561, 95 % ДІ **[0.446, 0.679]** |

## HTML-версія

Відкривається подвійним кліком. Керування: `←` `→` слайди, `O` — режим списку (зручно для
друку в PDF), `N` — нотатки доповідача, `R` — розкрити розбори кейсів, `T` — тема, `F` — повний екран.

## Фірмові візуальні елементи курсу

Два елементи навмисно уніфіковані в усіх матеріалах, бо повертатимуться в лекціях 4, 13 і 14:

* **колірна схема розбиття** — train сталево-синій, validation вохра, test сливовий,
  витік позначається тривожним червоним;
* **матриця плутанини** — той самий вигляд і та сама роль кольорів (TP зелений, FP червоний,
  FN вохра, TN нейтральний).

## Приклади

Усі числові приклади взяті з відкритих публікацій, переважно з оборонної предметної області
(виявлення БпЛА за радіо-, акустичним і відеоканалом, SAR ATR, кібербезпека) та частково
зі світу LLM-агентів (контамінація бенчмарків, калібрування після RLHF, детектори
промпт-ін'єкцій). Повний перелік джерел — на слайді 39 і в кінці конспекту.

Ключові пари чисел, на яких тримається лекція:

* DroneRF, тип дрона за РЧ-сигналом: macro-F1 **0.74 → 0.46** після leave-one-recording-out (arXiv:2607.01025);
* EchoHawk, акустичне виявлення: P_d@FAR 5 % **0.937 → 0.875** після групування за сесією запису (arXiv:2606.29589);
* SWE-bench Verified, пошук файлу з багом лише за текстом issue: **76 % → 53 %** на репозиторіях поза бенчмарком (arXiv:2506.12286);
* GPT-4 на підвибірці MMLU: ECE **0.007 → 0.074** після RLHF (GPT-4 Technical Report);
* PromptShield: **94.8 % → 65.3 %** виявлених ін'єкцій при зниженні FPR з 1 % до 0.1 % (arXiv:2501.15145).

Числа наведено так, як їх подають першоджерела. Частина робіт — препринти, тож перед лекцією
варто звірити свіжі версії.
